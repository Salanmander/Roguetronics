Prototype classes are classes that contain the information necessary to create a Node.

[[MachinePrototype]] is a base class. All specific instances will be of a class that inherits from it. It defines an array of [[ButtonPrototype|ButtonPrototypes]] that will be the buttons that get added to the interface when you have that machine. 