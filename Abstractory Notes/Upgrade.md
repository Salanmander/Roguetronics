Class that represents an increase in the capabilities the player has access to.

Associated with an ID. Stored in [[UpgradeTree]] class. Obtained and unobtained lists are just an ID. all_upgrades is a dictionary where the key is the ID and the value is an upgrade object.

Upgrade objects get passed in and out of [[UpgradeTree]].